spring:
  datasource:
    url: ${AUDIT_LOG_DB_URL:jdbc:postgresql://localhost:5432/audit_log}
    username: ${AUDIT_LOG_DB_USERNAME:audit_log}
    password: ${AUDIT_LOG_DB_PASSWORD:change-me}
    driver-class-name: org.postgresql.Driver
  h2:
    console:
      enabled: false

audit:
  security:
    password: ${AUDIT_LOG_API_PASSWORD}
