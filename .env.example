# Used only by the production PostgreSQL Docker Compose profile.
POSTGRES_DB=audit_log
POSTGRES_USER=audit_log
POSTGRES_PASSWORD=replace-with-a-strong-database-password
POSTGRES_PORT=5432
AUDIT_LOG_API_USERNAME=audit-service
AUDIT_LOG_API_PASSWORD=replace-with-a-strong-api-password
AUDIT_LOG_PORT=8080
