# AI Usage Log

| Date | Task | AI assistance | Disposition | Engineer rationale and validation |
| --- | --- | --- | --- | --- |
| 2026-08-14 | Initial repository scaffold, AI usage documentation, and attestation | Reviewed the starter project structure; updated the AI usage log from its template entry; and added a requirement to review the attestation and usage log before every commit and push. Constraint: do not create a Git commit. | Edited | Engineer will review all working-tree changes and perform the commit independently. No secrets, credentials, or confidential data were included. |
| 2026-08-14 | Milestone 1 foundation | Proposed and, after explicit approval, implemented the dependency baseline, H2 default configuration, PostgreSQL-ready profile, Flyway baseline, Docker Compose skeleton, and foundation documentation. | Edited | Engineer requested a milestone-by-milestone build, H2 for now, and PostgreSQL setup only. No credentials were committed. Validate the resolved dependency graph and application startup before committing. |
| 2026-08-14 | Milestone 2 Scenario A write/query | After explicit milestone approval, implemented the append and query slice, including canonical JSON hashing, transactional chain sequencing, Flyway schema, API validation, and focused unit tests. | Edited | Engineer selected server-assigned timestamps and a single global chain for a defensible prototype. Review the hash contract and validate with Java 21 before committing. |
| 2026-08-14 | Milestone 3 Scenario A verification | After explicit milestone approval, implemented full-chain verification, categorized failures, and an integration test that modifies a stored row directly. | Edited | Verification recomputes each event hash, validates linkage and the final chain state. Run the Java 21 test suite before committing. |
| 2026-08-14 | Milestone 4 Scenario B retention | After explicit milestone approval, implemented configurable soft archival, scheduling, archived-event query behavior, and verification coverage for archived records. | Edited | Soft archival preserves the immutable ledger and avoids a false chain break. Retention duration remains a deployment policy decision; run the Java 21 test suite before committing. |
| 2026-08-14 | Milestone 5 Scenario B redaction/export | After explicit milestone approval, implemented one-time scalar JSON Pointer redaction with a redaction certificate, certificate-aware verification, and self-contained export bundles. | Edited | Redaction removes the original scalar value from the online event row while preserving hash evidence. Export manifests are internally verifiable but unsigned; run the Java 21 test suite before committing. |
| 2026-08-14 | Milestone 6 Scenario C compliance reporting | After explicit milestone approval, identified ambiguities, recorded assumptions and scope boundaries, and implemented a narrow client-account access report with integrity status. | Edited | The endpoint intentionally does not guess authorization or jurisdiction rules. Run the Java 21 test suite before committing. |
