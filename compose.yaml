services:
  postgres:
    image: postgres:17-alpine
    profiles: ["prod"]
    environment:
      POSTGRES_DB: ${POSTGRES_DB:?Set POSTGRES_DB in .env}
      POSTGRES_USER: ${POSTGRES_USER:?Set POSTGRES_USER in .env}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?Set POSTGRES_PASSWORD in .env}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 5s
      timeout: 3s
      retries: 10
    volumes:
      - postgres-data:/var/lib/postgresql/data

  audit-log:
    build:
      context: .
    profiles: ["prod"]
    environment:
      SPRING_PROFILES_ACTIVE: prod
      AUDIT_LOG_DB_URL: jdbc:postgresql://postgres:5432/${POSTGRES_DB:?Set POSTGRES_DB in .env}
      AUDIT_LOG_DB_USERNAME: ${POSTGRES_USER:?Set POSTGRES_USER in .env}
      AUDIT_LOG_DB_PASSWORD: ${POSTGRES_PASSWORD:?Set POSTGRES_PASSWORD in .env}
      AUDIT_LOG_OIDC_ISSUER: ${AUDIT_LOG_OIDC_ISSUER:?Set AUDIT_LOG_OIDC_ISSUER in .env}
    ports:
      - "${AUDIT_LOG_PORT:-8080}:8080"
    depends_on:
      postgres:
        condition: service_healthy

volumes:
  postgres-data:
